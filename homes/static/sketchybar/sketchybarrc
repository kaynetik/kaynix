#!/usr/bin/env lua

-- Load the sketchybar-package and prepare the helper binaries
require("helpers")
require("init")

-- NOTE: SbarLua comes from nixpkgs (pkgs.sbarlua, Lua 5.5). LUA_CPATH is set for the
-- sketchybar LaunchAgent and in home.sessionVariables (see modules/apps.nix, zsh-config).
-- Manual: clone SbarLua, `make install`, or keep ~/.local/share/sketchybar_lua/sketchybar.so.
