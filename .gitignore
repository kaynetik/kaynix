# This repo lives at ~/.config. Only nix-darwin/ is source of truth;
# everything else is either HM-managed symlinks or legacy config.
*

# Whitelist nix-darwin and repo metadata
!.gitignore
!.pre-commit-config.yaml
!nix-darwin/
!nix-darwin/**
